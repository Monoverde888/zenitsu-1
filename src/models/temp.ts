/*

import mongoose from 'mongoose';
const { Schema, model } = mongoose;

const modelo = Schema({
    type: String,
    id: String,
    role: String,
    guild: String,
    toDelete: Number,
    tiempo: Number
})

export default model<Warns>('temp', modelo)

*/